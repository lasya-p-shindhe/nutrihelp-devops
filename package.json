{
  "name": "nutrihelp-api",
  "version": "1.0.0",
  "description": "NutriHelp backend API - nutrition app for elderly Australians",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && echo Build complete",
    "test": "jest --testTimeout=10000 --forceExit",
    "lint": "eslint . --ext .js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "eslint": "^8.57.0"
  }
}